services:
  sports-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HEADLESS: "true"
      RUNTIME_CONFIG_PATH: /app/config/config.json
    volumes:
      - ./config:/app/config
