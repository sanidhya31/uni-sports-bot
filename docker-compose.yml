services:
  sports-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      USER_DB_PATH: /app/users.db
    volumes:
      # Persist the encrypted user database across restarts.
      - ./users.db:/app/users.db
