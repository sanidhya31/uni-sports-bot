[project]
name = "uni-sports-bot"
version = "0.1.0"
description = "Playwright polling bot for Uni Trier sports slot availability"
requires-python = ">=3.11"
dependencies = [
    "apprise==1.10.0",
    "playwright==1.60.0",
    "python-dotenv==1.2.2",
]

[tool.uv]
package = false
